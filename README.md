# Grapevine-leaf-classifier-CNN
A Grapevine leaf classifier built using CNN, Transfer Learning (ResNet &amp; MobileNet), and SVM. Implements image preprocessing, feature extraction, and model evaluation to identify grapevine diseases from leaf images.
